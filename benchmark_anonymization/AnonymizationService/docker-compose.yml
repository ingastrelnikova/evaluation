services:
  anonymization-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    networks:
      - anon-network

networks:
  anon-network:
    driver: bridge